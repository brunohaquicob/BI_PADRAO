<div class="col-md-{{ $tamanhoCol }}">
    <div class="form-group">
        <input type="text" id="{{ $id }}" name="{{ $id }}" class="form-control" placeholder="{{ $placeholder }}" {{ $required }}>
    </div>
</div>
